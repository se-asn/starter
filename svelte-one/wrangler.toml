# wrangler.toml - Konfiguration für Cloudflare Workers und D1
name = "svelte-training-app"
compatibility_date = "2023-09-04"
compatibility_flags = ["nodejs_compat"]

# Konfiguration der Cloudflare D1 Datenbank
[[d1_databases]]
binding = "DB" # Dies ist der Name, der in deinem Code als env.DB verfügbar sein wird
database_name = "training_plans_db"
database_id = "30ca8fe2-fb17-4cfa-963e-a3eeda8cb5c5"

# Entwicklungsmodus für lokale Entwicklung
[env.development]
vars = { NODE_ENV = "development" }
workers_dev = true

# Produktionsmodus mit spezifischen Einstellungen
[env.production]  
vars = { NODE_ENV = "production" }
workers_dev = false

# Umgebungsvariablen
[vars]
APP_ENV = "development" # Wird in production automatisch überschrieben
# API Keys (werden in Cloudflare Dashboard als Secrets gesetzt)
# STRAVA_CLIENT_ID = "your_strava_client_id"
# STRAVA_CLIENT_SECRET = "your_strava_client_secret"
# GARMIN_CONSUMER_KEY = "your_garmin_key"
# GARMIN_CONSUMER_SECRET = "your_garmin_secret"
# POLAR_CLIENT_ID = "your_polar_client_id"
# POLAR_CLIENT_SECRET = "your_polar_client_secret"
# WAHOO_CLIENT_ID = "your_wahoo_client_id"
# WAHOO_CLIENT_SECRET = "your_wahoo_client_secret"
# JWT_SECRET = "your_jwt_secret_key"
